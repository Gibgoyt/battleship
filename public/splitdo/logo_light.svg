<svg width="400" height="600" viewBox="0 0 400 600" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Define clip path for rounded corners -->
  <defs>
    <clipPath id="rounded-clip">
      <rect x="80" y="240" width="240" height="240" rx="30"/>
    </clipPath>
  </defs>

  <!-- Circle (head) -->
  <circle cx="200" cy="120" r="80" fill="#6b46c1"/>

  <!-- Split square (body) with clip path for rounded corners -->
  <g clip-path="url(#rounded-clip)">
    <!-- Purple background -->
    <rect x="80" y="240" width="240" height="240" fill="#6b46c1"/>

    <!-- Deep purple triangle (bottom-right half) -->
    <path d="M 320 240 L 320 480 L 80 480 Z" fill="#2d1b4e"/>
  </g>
</svg>
