---
import Layout from 'src/layouts/Layout.astro';

export const prerender = true

// Fetch presale price at build time
let formattedPrice = '$0.001';
try {
  const response = await fetch('https://devbackend.splitdo.app:8443/api/splitdo-token/program/info', {
    headers: {
      'Origin': 'https://splitdo.app'
    }
  });
  if (response.ok) {
    const data = await response.json();
    if (data.success && data.data?.exchange_rate) {
      const price = data.data.exchange_rate;
      formattedPrice = price >= 1 ? `$${price.toFixed(2)}` : `$${price.toFixed(3)}`;
      console.info(`Presale price fetched: ${formattedPrice}`);
    }
  }
} catch (error) {
  console.warn('Failed to fetch presale price, using fallback:', error);
}
---

<Layout title="SPLITDO - Decentralized Expense Splitting">
  <main class="min-h-screen bg-zinc-900">

    <!-- Hero Section -->
    <section class="relative min-h-screen flex items-center">
      <!-- Subtle gradient background -->
      <div class="absolute inset-0 bg-gradient-to-b from-zinc-900 via-zinc-900 to-zinc-800"></div>
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[600px] bg-cyan-500/5 rounded-full blur-3xl"></div>
      
      <div class="relative w-full max-w-6xl mx-auto px-4 sm:px-6 py-20 sm:py-32">
        <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
          <!-- Left Content -->
          <div>
            <!-- Live Badge -->
            <div class="inline-flex items-center gap-2 mb-8">
              <span class="relative flex h-2 w-2">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-cyan-400 opacity-75"></span>
                <span class="relative inline-flex rounded-full h-2 w-2 bg-cyan-500"></span>
              </span>
              <span class="text-cyan-400 text-sm font-medium tracking-wide uppercase">Presale Live</span>
            </div>

            <!-- Main Heading -->
            <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6">
              The Future of<br/>
              <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500">Expense Splitting</span>
            </h1>

            <!-- Subtitle -->
            <p class="text-lg sm:text-xl text-zinc-400 mb-10 max-w-lg">
              Join the decentralized revolution. Secure your $SPLITDO tokens at presale price before public launch.
            </p>

            <!-- CTA Buttons -->
            <div class="flex flex-col sm:flex-row gap-4 mb-12">
              <a 
                href="/app/splitdo-exchange" 
                class="px-8 py-4 bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-cyan-400 hover:to-blue-500 text-white font-semibold text-center transition-all"
              >
                Join Presale
              </a>
              <a 
                href="/tokenomics" 
                class="px-8 py-4 text-zinc-300 hover:text-white font-semibold text-center border-b-2 border-transparent hover:border-cyan-500 transition-all"
              >
                View Tokenomics
              </a>
            </div>

            <!-- Countdown -->
            <div>
              <p class="text-xs text-zinc-500 uppercase tracking-widest mb-4">Phase 1 Ends In</p>
              <div id="countdown" class="flex gap-6 sm:gap-8">
                <div>
                  <div class="text-3xl sm:text-4xl font-bold text-white" id="days">00</div>
                  <div class="text-xs text-zinc-500 uppercase mt-1">Days</div>
                </div>
                <div class="text-3xl sm:text-4xl font-bold text-zinc-700">:</div>
                <div>
                  <div class="text-3xl sm:text-4xl font-bold text-white" id="hours">00</div>
                  <div class="text-xs text-zinc-500 uppercase mt-1">Hours</div>
                </div>
                <div class="text-3xl sm:text-4xl font-bold text-zinc-700">:</div>
                <div>
                  <div class="text-3xl sm:text-4xl font-bold text-white" id="minutes">00</div>
                  <div class="text-xs text-zinc-500 uppercase mt-1">Min</div>
                </div>
                <div class="text-3xl sm:text-4xl font-bold text-zinc-700">:</div>
                <div>
                  <div class="text-3xl sm:text-4xl font-bold text-cyan-400" id="seconds">00</div>
                  <div class="text-xs text-zinc-500 uppercase mt-1">Sec</div>
                </div>
              </div>
            </div>
          </div>

          <!-- Right: Phone Mockup -->
          <div class="flex justify-center lg:justify-end">
            <div class="relative w-[280px] sm:w-[320px]">
              <!-- Phone frame -->
              <div class="relative bg-zinc-800 rounded-[3rem] p-2 shadow-2xl shadow-black/50">
                <!-- Notch -->
                <div class="absolute top-0 left-1/2 -translate-x-1/2 w-24 h-6 bg-zinc-800 rounded-b-2xl z-10 flex justify-center items-center">
                  <div class="w-12 h-1.5 bg-zinc-900 rounded-full"></div>
                </div>
                <!-- Screen -->
                <div class="relative overflow-hidden rounded-[2.5rem] bg-zinc-900">
                  <img
                    id="dark-phone-img"
                    src="/phone_screens/WalletScreen_DARK.png?v=2"
                    alt="SplitDo App"
                    class="w-full aspect-[9/19] object-cover"
                  />
                </div>
              </div>
              <!-- Glow effect -->
              <div class="absolute -inset-4 bg-cyan-500/10 rounded-[4rem] blur-2xl -z-10"></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Stats Section -->
    <section class="border-y border-zinc-800">
      <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="grid grid-cols-2 lg:grid-cols-4">
          <div class="py-8 sm:py-12 text-center border-r border-zinc-800 last:border-r-0">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-white mb-1">100M</div>
            <div class="text-sm text-zinc-500">Total Supply</div>
          </div>
          <div class="py-8 sm:py-12 text-center border-r border-zinc-800 lg:border-r">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-white mb-1">31.5M</div>
            <div class="text-sm text-zinc-500">Strategic Sale</div>
          </div>
          <div class="py-8 sm:py-12 text-center border-r border-zinc-800">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-white mb-1">50K+</div>
            <div class="text-sm text-zinc-500">Early Users</div>
          </div>
          <div class="py-8 sm:py-12 text-center">
            <div class="text-2xl sm:text-3xl lg:text-4xl font-bold text-cyan-400 mb-1">{formattedPrice}</div>
            <div class="text-sm text-zinc-500">Presale Price</div>
          </div>
        </div>
      </div>
    </section>

    <!-- Why SPLITDO Section -->
    <section class="py-20 sm:py-32">
      <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="max-w-2xl mb-16">
          <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">
            Why $SPLITDO?
          </h2>
          <p class="text-lg text-zinc-400">
            More than a token. Your key to a revolutionary expense management ecosystem.
          </p>
        </div>

        <div class="grid md:grid-cols-3 gap-12 md:gap-16">
          <!-- Feature 1 -->
          <div>
            <div class="w-12 h-12 bg-cyan-500/10 flex items-center justify-center mb-6">
              <svg class="w-6 h-6 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"/>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-white mb-3">Reduced Fees</h3>
            <p class="text-zinc-500 leading-relaxed">
              Pay significantly lower transaction fees and unlock premium features with $SPLITDO tokens.
            </p>
          </div>

          <!-- Feature 2 -->
          <div>
            <div class="w-12 h-12 bg-cyan-500/10 flex items-center justify-center mb-6">
              <svg class="w-6 h-6 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"/>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-white mb-3">Governance Power</h3>
            <p class="text-zinc-500 leading-relaxed">
              Vote on platform decisions, new features, and protocol updates. Shape the future of SplitDo.
            </p>
          </div>

          <!-- Feature 3 -->
          <div>
            <div class="w-12 h-12 bg-cyan-500/10 flex items-center justify-center mb-6">
              <svg class="w-6 h-6 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-white mb-3">Staking Rewards</h3>
            <p class="text-zinc-500 leading-relaxed">
              Stake tokens to earn rewards while supporting network security and decentralization.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Community Section -->
    <section class="py-20 sm:py-32 bg-zinc-800/30">
      <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
          <!-- Image -->
          <div class="relative">
            <img
              src="/Humans.jpeg"
              alt="SplitDo Community"
              class="w-full aspect-[4/3] object-cover"
              loading="lazy"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-zinc-900/80 via-transparent to-transparent"></div>
            <div class="absolute bottom-0 left-0 right-0 p-6 sm:p-8">
              <div class="text-2xl sm:text-3xl font-bold text-white mb-2">50,000+</div>
              <div class="text-zinc-400">Community Members</div>
            </div>
          </div>

          <!-- Content -->
          <div>
            <h2 class="text-3xl sm:text-4xl font-bold text-white mb-6">
              Built by the Community
            </h2>
            <p class="text-lg text-zinc-400 mb-10">
              SplitDo is more than an app. Join thousands transforming how they handle group expenses.
            </p>

            <div class="space-y-8">
              <div class="flex gap-4">
                <div class="w-1 bg-cyan-500 flex-shrink-0"></div>
                <div>
                  <h3 class="text-lg font-semibold text-white mb-1">Active Community</h3>
                  <p class="text-zinc-500">Join discussions, share feedback, and help shape the future.</p>
                </div>
              </div>

              <div class="flex gap-4">
                <div class="w-1 bg-cyan-500 flex-shrink-0"></div>
                <div>
                  <h3 class="text-lg font-semibold text-white mb-1">Early Adopter Benefits</h3>
                  <p class="text-zinc-500">Exclusive access to features and special token rewards.</p>
                </div>
              </div>

              <div class="flex gap-4">
                <div class="w-1 bg-cyan-500 flex-shrink-0"></div>
                <div>
                  <h3 class="text-lg font-semibold text-white mb-1">Governance Rights</h3>
                  <p class="text-zinc-500">Decide the future direction through token governance.</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- How to Join Section -->
    <section class="py-20 sm:py-32">
      <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="max-w-2xl mb-16">
          <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">
            How to Join
          </h2>
          <p class="text-lg text-zinc-400">
            Three simple steps to secure your $SPLITDO tokens.
          </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8 md:gap-12">
          <!-- Step 1 -->
          <div class="relative">
            <div class="text-6xl sm:text-7xl font-bold text-zinc-800 mb-4">01</div>
            <h3 class="text-xl font-semibold text-white mb-3">Connect Wallet</h3>
            <p class="text-zinc-500">
              Connect your Solana wallet. We support Phantom, Solflare, and all major wallets.
            </p>
          </div>

          <!-- Step 2 -->
          <div class="relative">
            <div class="text-6xl sm:text-7xl font-bold text-zinc-800 mb-4">02</div>
            <h3 class="text-xl font-semibold text-white mb-3">Choose Amount</h3>
            <p class="text-zinc-500">
              Select how many $SPLITDO tokens you want to purchase at presale price.
            </p>
          </div>

          <!-- Step 3 -->
          <div class="relative">
            <div class="text-6xl sm:text-7xl font-bold text-zinc-800 mb-4">03</div>
            <h3 class="text-xl font-semibold text-white mb-3">Complete Purchase</h3>
            <p class="text-zinc-500">
              Confirm transaction and receive tokens directly in your wallet.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Token Distribution Section -->
    <section class="py-20 sm:py-32 bg-zinc-800/30">
      <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="max-w-2xl mb-16">
          <h2 class="text-3xl sm:text-4xl font-bold text-white mb-4">
            Token Distribution
          </h2>
          <p class="text-lg text-zinc-400">
            Fair and transparent allocation designed for long-term growth.
          </p>
        </div>

        <div class="grid grid-cols-2 lg:grid-cols-4 gap-8 sm:gap-12 mb-12">
          <div>
            <div class="text-4xl sm:text-5xl font-bold text-cyan-400 mb-2">31.5%</div>
            <div class="text-lg font-semibold text-white mb-1">Strategic Sale</div>
            <div class="text-sm text-zinc-500">Early investors & partnerships</div>
          </div>
          <div>
            <div class="text-4xl sm:text-5xl font-bold text-white mb-2">25%</div>
            <div class="text-lg font-semibold text-white mb-1">Liquidity</div>
            <div class="text-sm text-zinc-500">DEX pools & market making</div>
          </div>
          <div>
            <div class="text-4xl sm:text-5xl font-bold text-white mb-2">23.5%</div>
            <div class="text-lg font-semibold text-white mb-1">Development</div>
            <div class="text-sm text-zinc-500">Team & ongoing development</div>
          </div>
          <div>
            <div class="text-4xl sm:text-5xl font-bold text-white mb-2">20%</div>
            <div class="text-lg font-semibold text-white mb-1">Ecosystem</div>
            <div class="text-sm text-zinc-500">Rewards & marketing</div>
          </div>
        </div>

        <a 
          href="/tokenomics" 
          class="inline-flex items-center gap-2 text-cyan-400 hover:text-cyan-300 font-medium transition-colors"
        >
          View Full Tokenomics
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </a>
      </div>
    </section>

    <!-- Final CTA Section -->
    <section class="py-20 sm:py-32">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 text-center">
        <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-6">
          Ready to Join the Future?
        </h2>
        <p class="text-lg text-zinc-400 mb-10">
          Secure your $SPLITDO tokens at presale price. This opportunity won't last.
        </p>
        <a 
          href="/app/splitdo-exchange" 
          class="inline-block px-10 py-4 bg-gradient-to-r from-cyan-500 to-blue-600 hover:from-cyan-400 hover:to-blue-500 text-white text-lg font-semibold transition-all"
        >
          Join Presale Now
        </a>
      </div>
    </section>

  </main>

  <!-- Sticky Bottom Bar -->
  <div class="fixed bottom-0 left-0 right-0 z-40 bg-zinc-900/95 backdrop-blur-sm border-t border-zinc-800">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 py-3 sm:py-4 flex items-center justify-between">
      <div>
        <div class="text-xs text-zinc-500 uppercase tracking-wider">Presale Price</div>
        <div class="text-lg sm:text-xl font-bold text-cyan-400">{formattedPrice}</div>
      </div>
      <a 
        href="/app/splitdo-exchange" 
        class="px-6 py-2.5 bg-cyan-500 hover:bg-cyan-400 text-zinc-900 font-semibold transition-colors"
      >
        Join Now
      </a>
    </div>
  </div>

  <script>
    // Countdown Timer
    function updateCountdown() {
      const targetDate = new Date('2025-12-31T23:59:59').getTime();
      const now = new Date().getTime();
      const distance = targetDate - now;

      if (distance > 0) {
        const days = Math.floor(distance / (1000 * 60 * 60 * 24));
        const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((distance % (1000 * 60)) / 1000);

        const daysEl = document.getElementById('days');
        const hoursEl = document.getElementById('hours');
        const minutesEl = document.getElementById('minutes');
        const secondsEl = document.getElementById('seconds');

        if (daysEl) daysEl.textContent = days.toString().padStart(2, '0');
        if (hoursEl) hoursEl.textContent = hours.toString().padStart(2, '0');
        if (minutesEl) minutesEl.textContent = minutes.toString().padStart(2, '0');
        if (secondsEl) secondsEl.textContent = seconds.toString().padStart(2, '0');
      }
    }

    setInterval(updateCountdown, 1000);
    updateCountdown();
  </script>
</Layout>
