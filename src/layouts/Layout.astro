---
import DarkModeToggle from 'src/components-qwik/DarkModeToggle';
import 'src/styles/global.css';

// Define custom colors as a data object for use in the template
const customColors = {
    background: '#564740', // Dark Background (YOUR COLOR)
    primary: '#f07d40',    // Primary Orange Accent
    secondary: '#47a49e',  // Secondary Teal Accent
    // A slightly darker shade of primary for hover state
    primaryDark: '#d86e37', 
};

// opt into SSG
export const prerender = true

export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    
    <style define:vars={customColors}>
        /* Define custom CSS variables globally from the passed frontmatter variables */
        :root {
            --color-background: var(--background); /* Exposing background color as a variable */
            --color-primary: var(--primary);
            --color-secondary: var(--secondary);
            --color-primary-dark: var(--primaryDark);
            
            /* Add custom utility classes for the header colors */
            .dark .bg-header-dark {
                background-color: var(--color-background);
            }
            .dark .border-header-dark {
                border-color: var(--color-secondary); /* Using secondary for dark mode border */
            }
            .focus-ring-primary:focus {
                --tw-ring-color: var(--color-primary);
            }
        }

        /* Custom Tailwind class to use CSS variable for smooth hover */
        .bg-primary-hover:hover {
            background-color: var(--color-primary-dark) !important;
        }

        /* Apply a subtle dark theme to the base body for consistency */
        body {
            background-color: #f7f7f7; /* Light background for light mode */
        }
        /* Now uses the CSS variable --background for the dark mode body color */
        .dark body {
            background-color: var(--background); 
        }
    </style>
    
    <script is:inline>
        // Prevent flash of light mode
        const isDarkMode = localStorage.getItem('darkMode') === 'true' ||
            (!localStorage.getItem('darkMode') && window.matchMedia('(prefers-color-scheme: dark)').matches);

        if (isDarkMode) {
            document.documentElement.classList.add('dark');
        }
    </script>
</head>
<body class="text-gray-900 dark:text-gray-100 transition-colors duration-300">
<header class="border-b border-[#47a49e] dark:border-header-dark bg-[#f07d40] dark:bg-header-dark shadow-md">
    <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <div class="flex items-center space-x-4">
                <a href="/" class="flex items-center">
                    <img src="/splitdo/full_logo.JPG" alt="SplitDo Company Logo" class="h-16 w-auto">
                </a>
            </div>

            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="text-black hover:scale-120 transition-transform">Home</a>
                <a href="/features" class="text-black hover:scale-120 transition-transform">Features</a>
                <a href="/pricing" class="text-black hover:scale-120 transition-transform">Download</a>
                <a href="/about" class="text-black hover:scale-120 transition-transform">About Us</a>
                                
                <a href="/app/dashboard" 
                   class="px-4 py-2 text-white rounded-lg transition-colors shadow-md bg-primary-hover"
                   style={`background-color: ${customColors.primary};`}>
                    Go To App
                </a>
            </div>

            <div class="flex items-center space-x-2">
                <div class="hidden md:block">
                    <DarkModeToggle client:load />
                </div>
                <div class="md:hidden">
                    <button
                        id="mobile-menu-button"
                        type="button"
                        class="text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 focus:outline-none focus:ring-2 focus-ring-primary focus:ring-offset-2 dark:focus:ring-offset-gray-800 p-2"
                        aria-controls="mobile-menu"
                        aria-expanded="false"
                    >
                        <span class="sr-only">Open main menu</span>
                        <svg class="h-6 w-6" id="menu-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                        <svg class="h-6 w-6 hidden" id="close-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <div class="md:hidden hidden" id="mobile-menu">
            <div class="px-2 pt-2 pb-3 space-y-1 border-t border-gray-200 dark:border-header-dark bg-white dark:bg-header-dark">
                <a href="/" class="block px-3 py-2 text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-50 dark:hover:bg-gray-700 rounded-md transition-colors">Home</a>
                <a href="/features" class="block px-3 py-2 text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-50 dark:hover:bg-gray-700 rounded-md transition-colors">Features</a>
                <a href="/pricing" class="block px-3 py-2 text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-50 dark:hover:bg-gray-700 rounded-md transition-colors">Download</a>
                <a href="/about" class="block px-3 py-2 text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-50 dark:hover:bg-gray-700 rounded-md transition-colors">About</a>
                <a href="/privacy" class="block px-3 py-2 text-gray-700 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 hover:bg-gray-50 dark:hover:bg-gray-700 rounded-md transition-colors">Privacy</a>
                
                <a href="/app/dashboard" 
                   class="block mx-3 my-2 px-4 py-2 text-white rounded-lg transition-colors text-center shadow-md bg-primary-hover"
                   style={`background-color: ${customColors.primary};`}>
                    Go To App
                </a>

                <div class="flex justify-center py-2">
                    <DarkModeToggle client:load />
                </div>
            </div>
        </div>
    </nav>
</header>

<slot />

<!-- FOOTER: Added after slot and styled using CSS variables -->
<footer class="border-t dark:border-gray-800 py-12" 
        style="background-color: var(--color-primary); border-color: var(--color-background);">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center text-white dark:text-gray-200">
            <p>&copy; 2025 SplitDo. All rights reserved.</p>
        </div>
    </div>
</footer>

<script>
    // Mobile menu toggle functionality... (rest of the script is unchanged)
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    const menuIcon = document.getElementById('menu-icon');
    const closeIcon = document.getElementById('close-icon');

    if (mobileMenuButton && mobileMenu && menuIcon && closeIcon) {
        mobileMenuButton.addEventListener('click', () => {
            const isOpen = !mobileMenu.classList.contains('hidden');

            if (isOpen) {
                // Close menu
                mobileMenu.classList.add('hidden');
                menuIcon.classList.remove('hidden');
                closeIcon.classList.add('hidden');
                mobileMenuButton.setAttribute('aria-expanded', 'false');
            } else {
                // Open menu
                mobileMenu.classList.remove('hidden');
                menuIcon.classList.add('hidden');
                closeIcon.classList.remove('hidden');
                mobileMenuButton.setAttribute('aria-expanded', 'true');
            }
        });
    }
</script>
</body>
</html>